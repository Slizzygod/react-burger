const APP_HEADER_HEIGHT = 80;
const NEEDLY_BOTTOM_HEIGHT = 10;

const SECTION_HEIGHT =
  document.documentElement.clientHeight -
  APP_HEADER_HEIGHT -
  NEEDLY_BOTTOM_HEIGHT;

export { SECTION_HEIGHT };
